context:
  name: rhppandas
  version: "0.3.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: 775ed4af085107fe913f98393c2b54dde69195b3ad7aa7f2db6a45d46bfd5f02

build:
  noarch: python
  script:
    - python -m pip install . --no-deps -vv
  number: 0

requirements:
  host:
    - python ==${{ python_min }}
    - poetry-core >=2.0,<3.0
    - wheel
    - pip
  run:
    - python >=${{ python_min }}
    - rhealpixdggs >=0.7.1
    - geopandas
    - numpy
    - pandas
    - shapely
    - typing-extensions

tests:
  - python:
      imports:
        - rhppandas
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"
  - script:
      - pytest -v tests/
    requirements:
      run:
        - pytest
        - python ==${{ python_min }}
    files:
      source:
        - tests/

about:
  homepage: "https://github.com/manaakiwhenua/rHP-Pandas"
  repository: "https://github.com/manaakiwhenua/rHP-Pandas"
  summary: "Integration of rHEALPixDGGS and Pandas/GeoPandas"
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - alpha-beta-soup
  tags:
    - rhealpix
    - DGGS
    - Discrete Global Grid System
    - geospatial
    - spatial
    - reference system
